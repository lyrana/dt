// Tester program

#include "SegmentedArrayPair_Cpp.h"

using namespace dnt;

int main() { 

  // Create a SegmentedArrayPair object
  py::ssize_t segmentLength = 100;
  
  SegmentedArrayPair<DnT_pstruct1D> seg_array_obj(segmentLength);
  
	return 0; 
} 
